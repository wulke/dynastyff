# @spec DFF-STATIC-040
# @spec DFF-STATIC-041
# @spec DFF-STATIC-042
# @spec DFF-STATIC-043
# @spec DFF-STATIC-044
name: ETL Snapshot

on:
  workflow_dispatch:

jobs:
  etl-snapshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - run: npm run etl

      - run: npm run export:snapshot

      - id: snapshot_diff
        run: |
          if git diff --quiet --exit-code -- data/snapshot.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - if: steps.snapshot_diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/snapshot.json
          git commit -m "chore: refresh static snapshot"
          git push origin HEAD:${{ github.ref_name }}
